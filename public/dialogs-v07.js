function openLot(id){const l=state.lots.find(x=>x.id===id);if(!l)return;const a=auctionFor(l),x=listingFor(l);const seller=x?sellerFor(x.sellerId):null;let commerce='';if(a)commerce=`<div class="bid-box"><div class="bid-line"><span>${tr('currentBid')}</span><strong>${money(a.currentBid,a.currency)} · ${a.bidCount} ${tr('bids')}</strong></div><div class="bid-line"><span>${tr('closes')}</span><span>${dt(a.endsAt)} · ${countdown(a.endsAt)}</span></div><div class="bid-line"><span>Reserve</span><span class="reserve ${a.reserveMet?'met':''}">${a.reserveMet?tr('reserveMet'):tr('reserveNotMet')}</span></div>${state.persona==='BUYER'?`<div class="bid-control"><input id="bidInput" type="number" min="${a.currentBid+a.increment}" value="${a.currentBid+a.increment}"><button class="primary-button" data-bid="${a.id}">${tr('placeMax')}</button></div>`:`<div class="notice-inline">${tr('sellerCannotBid')}</div>`}<div class="sale-meta">${tr('maxConfidential')} ${tr('previewBid')}</div></div>`;if(x)commerce=`<div class="commerce-box"><div class="price-large">${money(x.price,x.currency)}</div><div class="availability"><span class="status positive">${tr('available')}</span>${x.negotiable?`<span>${tr('negotiable')}</span>`:''}</div>${state.persona==='BUYER'?`<div class="commerce-actions">${x.saleType==='BUY_NOW'?`<button class="primary-button" data-buy="${x.id}">${tr('buyNow')}</button>`:''}${x.negotiable?`<button class="secondary-button" data-offer="${x.id}">${tr('makeOffer')}</button>`:''}<button class="secondary-button" data-message="${x.id}">${tr('askSeller')}</button></div>`:`<div class="notice-inline">${state.lang==='ru'?'Переключитесь в кабинет покупателя для покупки или предложения цены.':'Switch to buyer cabinet to purchase or make an offer.'}</div>`}</div><div class="dealer-card"><div><div class="eyebrow">${tr('seller')}</div><h3>${seller?.name||''}</h3><p>${local(seller?.about)}</p><div class="dealer-meta"><span>${seller?.verified?'✓ '+tr('verifiedDealer'):''}</span><span>${local(seller?.city)} · ${tr('since')} ${seller?.since}</span><span>${tr('response')}: ${local(seller?.responseTime)}</span></div></div></div>`;$('#lotDialogBody').innerHTML=`<div class="lot-detail"><div class="lot-gallery"><img src="${img(l)}" alt="${local(l.title)}"><div class="thumbnail-row"><img src="${img(l)}"></div></div><div class="lot-panel"><button class="dialog-close" data-close>×</button><div class="eyebrow">${a?'Lot '+l.lotNumber:local(l.department)}</div><h2 class="lot-title">${local(l.maker)}<br><em>${local(l.title)}</em></h2><div class="sale-meta">${local(l.period)} · ${local(l.origin)}</div>${a?`<div class="lot-estimate-large">${tr('estimate')} ${money(l.estimateLow,l.currency)}–${money(l.estimateHigh,l.currency)}</div>`:''}${commerce}<div class="lot-actions"><button class="secondary-button" data-save="${l.id}">${state.client?.savedLots?.includes(l.id)?tr('saved')+' ♥':tr('save')+' ♡'}</button><button class="secondary-button" data-collect="${l.id}">${state.client?.collection?.includes(l.id)?tr('inCollection')+' ✓':tr('addCollection')}</button>${a?`<button class="secondary-button" data-alert="${l.id}">${state.client?.watchAlerts?.includes(l.id)?tr('alertOn'):tr('alert')}</button>`:''}</div><div class="accordion"><details open><summary>${tr('details')}</summary><div class="detail-copy">${local(l.cataloguing)}</div></details><details><summary>${tr('provenance')}</summary><div class="detail-copy">${(local(l.provenance)||[]).join('<br>')}</div></details><details><summary>${tr('condition')}</summary><div class="detail-copy">${local(l.condition)}</div></details>${a?`<details><summary>${tr('bidHistory')}</summary><div class="detail-copy">${(a.history||[]).slice().reverse().map(h=>`${h.bidder} · ${money(h.amount,a.currency)} · ${dt(h.time)}`).join('<br>')||tr('noHistory')}</div></details>`:''}<details><summary>${tr('buyerInfo')}</summary><div class="detail-copy">${tr('buyerPremium')}</div></details></div></div></div>`;$('#lotDialog').showModal();bindDynamic($('#lotDialogBody'))}
function showAction(html){$('#actionDialogBody').innerHTML=html;$('#actionDialog').showModal();bindDynamic($('#actionDialogBody'))}
function offerDialog(listingId){const x=state.listings.find(v=>v.id===listingId),l=state.lots.find(v=>v.id===x.lotId);showAction(`<form id="offerForm" class="registration-form"><div class="registration-head"><div><div class="eyebrow">${tr('makeOffer')}</div><h2>${local(l.title)}</h2></div><button type="button" class="close-button" data-action-close>×</button></div><div class="price-reference">${tr('askingPrice')}: <strong>${money(x.price,x.currency)}</strong></div><label class="field"><span>${tr('amount')}</span><input id="offerAmount" type="number" required min="1" max="${x.price-1}" value="${Math.round(x.price*.9)}"></label><div class="dialog-actions"><button type="button" class="secondary-button" data-action-close>${tr('cancel')}</button><button class="primary-button">${tr('makeOffer')}</button></div></form>`);$('#offerForm').onsubmit=async e=>{e.preventDefault();try{await api(`/api/listings/${listingId}/offers`,{method:'POST',body:JSON.stringify({amount:Number($('#offerAmount').value)})});toast(tr('offerSent'));$('#actionDialog').close();await refreshClient();openLot(x.lotId)}catch(err){toast(err.message)}}}
function messageDialog(listingId){const x=state.listings.find(v=>v.id===listingId),l=state.lots.find(v=>v.id===x.lotId);showAction(`<form id="messageForm" class="registration-form"><div class="registration-head"><div><div class="eyebrow">${tr('askSeller')}</div><h2>${local(l.title)}</h2></div><button type="button" class="close-button" data-action-close>×</button></div><label class="field"><span>${tr('message')}</span><textarea id="sellerMessage" required>${state.lang==='ru'?'Здравствуйте. Подскажите, пожалуйста, дополнительную информацию о состоянии и провенансе предмета.':'Hello. Could you please provide more information about the condition and provenance of this object?'}</textarea></label><div class="dialog-actions"><button type="button" class="secondary-button" data-action-close>${tr('cancel')}</button><button class="primary-button">${tr('send')}</button></div></form>`);$('#messageForm').onsubmit=async e=>{e.preventDefault();try{await api(`/api/listings/${listingId}/message`,{method:'POST',body:JSON.stringify({message:$('#sellerMessage').value})});toast(tr('enquiryCaptured'));$('#actionDialog').close()}catch(err){toast(err.message)}}}
function newListingDialog(){showAction(`<form id="newListingForm" class="registration-form wide-form"><div class="registration-head"><div><div class="eyebrow">${tr('sellerCabinet')}</div><h2>${tr('createListing')}</h2></div><button type="button" class="close-button" data-action-close>×</button></div><div class="two-col"><label class="field"><span>Title EN</span><input id="nlTitleEn" required></label><label class="field"><span>Название RU</span><input id="nlTitleRu" required></label><label class="field"><span>Category EN</span><input id="nlCatEn" value="Decorative Arts" required></label><label class="field"><span>Категория RU</span><input id="nlCatRu" value="Декоративное искусство" required></label><label class="field"><span>${tr('price')} EUR</span><input id="nlPrice" type="number" value="5000" required></label><label class="field"><span>${tr('saleType')}</span><select id="nlType"><option value="BUY_NOW">BUY NOW</option><option value="MAKE_OFFER">MAKE OFFER</option></select></label><label class="field full"><span>Image URL</span><input id="nlImage" value="https://images.metmuseum.org/CRDImages/es/web-large/DP340468.jpg"></label></div><div class="dialog-actions"><button type="button" class="secondary-button" data-action-close>${tr('cancel')}</button><button class="primary-button">${tr('createListing')}</button></div></form>`);$('#newListingForm').onsubmit=async e=>{e.preventDefault();try{await api('/api/seller/listings',{method:'POST',body:JSON.stringify({titleEn:$('#nlTitleEn').value,titleRu:$('#nlTitleRu').value,categoryEn:$('#nlCatEn').value,categoryRu:$('#nlCatRu').value,price:Number($('#nlPrice').value),saleType:$('#nlType').value,image:$('#nlImage').value,negotiable:true})});toast(tr('draftReview'));$('#actionDialog').close();await load();state.route='account';state.accountTab='inventory';render()}catch(err){toast(err.message)}}}
