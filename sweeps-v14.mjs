import {db,notify} from './runtime-v09.mjs';
import {transitionSettlement} from './domain-e2e-v14.mjs';
const SYSTEM={id:'system',roles:['ADMIN'],sellerId:null};
export async function sweepOverdueSettlements(){if(db.kind!=='POSTGRES')return{changed:0,reason:'MEMORY_PREVIEW'};const rows=(await db.pool.query("SELECT id FROM auction_settlements WHERE status='PAYMENT_DUE' AND payment_due_at IS NOT NULL AND payment_due_at<=clock_timestamp() ORDER BY payment_due_at LIMIT 100")).rows;let changed=0;for(const r of rows){try{await transitionSettlement(SYSTEM,r.id,'NONPAYMENT',{reason:'PAYMENT_DUE_EXPIRED',automatic:true});changed++}catch(e){console.error('nonpayment sweep',r.id,e.message)}}return{changed}}
export async function sweepExpiredInsurance(){if(db.kind!=='POSTGRES')return{changed:0,reason:'MEMORY_PREVIEW'};const rows=(await db.pool.query("UPDATE insurance_policies SET status='EXPIRED',updated_at=now() WHERE status='ACTIVE' AND expires_at IS NOT NULL AND expires_at<=clock_timestamp() RETURNING id,record_id,provider,expires_at")).rows;for(const p of rows){const r=(await db.pool.query('SELECT account_id,object_id FROM collection_records WHERE id=$1',[p.record_id])).rows[0];if(r)await notify(r.account_id,'INSURANCE_EXPIRED',{policyId:p.id,objectId:r.object_id,provider:p.provider,expiredAt:p.expires_at?.toISOString?.()||p.expires_at})}return{changed:rows.length}}
export async function runOperationalSweeps(){const [settlements,insurance]=await Promise.all([sweepOverdueSettlements(),sweepExpiredInsurance()]);return{settlements,insurance}}
