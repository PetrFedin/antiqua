import {db,orders,listings,uid,now} from './runtime-v09.mjs';
import {inferLifecycleAuthority,planLifecycleTransition} from './lifecycle-authority-v16.mjs';
import {findLifecycleEventBySourceTx,recordLifecycleTransitionTx} from './lifecycle-events-v16.mjs';
import {emitListingStatusMemory,enqueueListingStatusTx} from './watch-events-v19.mjs';

const conflict=message=>Object.assign(new Error(message),{status:409,code:'LIFECYCLE_EVENT_CONFLICT'});
const unavailable=()=>Object.assign(new Error('Item unavailable'),{status:409,code:'ITEM_UNAVAILABLE'});
const iso=v=>v?.toISOString?.()||v||null;
const mapVerification=r=>r?{id:r.id,accountId:r.account_id??r.accountId,subjectType:r.subject_type??r.subjectType,caseType:r.case_type??r.caseType,status:r.status,provider:r.provider||null,providerReference:r.provider_reference??r.providerReference??null,riskLevel:r.risk_level??r.riskLevel??null,decisionReason:r.decision_reason??r.decisionReason??'',submittedAt:iso(r.submitted_at??r.submittedAt),decidedAt:iso(r.decided_at??r.decidedAt),expiresAt:iso(r.expires_at??r.expiresAt),createdAt:iso(r.created_at??r.createdAt),updatedAt:iso(r.updated_at??r.updatedAt)}:null;
const orderFromRow=r=>r?{...(r.payload||{}),id:r.id,listingId:r.listing_id??r.payload?.listingId??null,lotId:r.object_id??r.payload?.lotId,buyerClientId:r.buyer_account_id??r.payload?.buyerClientId,sellerId:r.seller_id??r.payload?.sellerId,status:r.status,createdAt:iso(r.created_at??r.payload?.createdAt),updatedAt:iso(r.updated_at)}:null;
const orderAccess=(a,o)=>Boolean(a&&(a.id===o.buyerClientId||(a.sellerId&&a.sellerId===o.sellerId)||a.roles?.includes('ADMIN')||a.roles?.includes('TRUST_REVIEWER')));
const verificationAccess=(a,v)=>Boolean(a&&(a.id===v.accountId||a.roles?.includes('ADMIN')||a.roles?.includes('TRUST_REVIEWER')));
const privileged=a=>['SYSTEM','PROVIDER'].includes(String(a||'').toUpperCase());
function syncListing(id,status){const li=id?listings.get(id):null;if(li)li.status=status}
async function reserveListingMemory(id){if(!id)return null;const li=listings.get(id);if(!li||li.status!=='ACTIVE')throw unavailable();li.status='RESERVED';await db.putListing(li);return li}
async function reactivateListingMemory(id){if(!id)return null;const li=listings.get(id);if(li?.status==='RESERVED'){li.status='ACTIVE';await db.putListing(li)}return li||null}
async function reserveListingTx(cx,id){if(!id)return null;const row=(await cx.query('SELECT * FROM listings WHERE id=$1 FOR UPDATE',[id])).rows[0];if(!row||row.status!=='ACTIVE')throw unavailable();const payload={...(row.payload||{}),status:'RESERVED'};await cx.query("UPDATE listings SET status='RESERVED',payload=$2,updated_at=now() WHERE id=$1",[id,payload]);return row}
async function reactivateListingTx(cx,id){if(!id)return null;const row=(await cx.query('SELECT * FROM listings WHERE id=$1 FOR UPDATE',[id])).rows[0];if(row?.status==='RESERVED'){const payload={...(row.payload||{}),status:'ACTIVE'};await cx.query("UPDATE listings SET status='ACTIVE',payload=$2,updated_at=now() WHERE id=$1",[id,payload])}return row||null}

export async function loadOrderAuthority(id){
  let o=orders.get(id);if(o)return structuredClone(o);
  if(db.kind!=='POSTGRES')return null;
  const row=(await db.pool.query('SELECT * FROM orders WHERE id=$1',[id])).rows[0];o=orderFromRow(row);if(o)orders.set(o.id,o);return o?structuredClone(o):null
}

export async function createOrderAuthority(actor,order,{sourceKey=null,lifecycleAuthority=null,watchExcludeAccountIds=[]}={}){
  const authority=String(lifecycleAuthority||inferLifecycleAuthority({account:actor,resource:order})).toUpperCase(),plan=planLifecycleTransition({domain:'ORDER',from:'NONE',to:'AWAITING_PAYMENT_CONNECTOR',action:'CREATE',authority}),o={...order,status:plan.to};
  if(db.kind!=='POSTGRES'){
    const listingBefore=o.listingId?structuredClone(listings.get(o.listingId)||null):null;await reserveListingMemory(o.listingId);const listingAfter=o.listingId?structuredClone(listings.get(o.listingId)||null):null;
    orders.set(o.id,structuredClone(o));await db.putOrder(o);
    if(listingBefore&&listingAfter)await emitListingStatusMemory(listingBefore,listingAfter,{excludeAccountIds:[actor?.id,...watchExcludeAccountIds]});
    return{...structuredClone(o),idempotentTransition:false,lifecycle:plan}
  }
  const cx=await db.pool.connect();let reserved=false;try{
    await cx.query('BEGIN');const existing=(await cx.query('SELECT * FROM orders WHERE id=$1 FOR UPDATE',[o.id])).rows[0];if(existing){if(sourceKey){const prior=await findLifecycleEventBySourceTx(cx,{domain:'ORDER',aggregateId:o.id,sourceKey});if(prior?.to===plan.to){await cx.query('COMMIT');const current=orderFromRow(existing);orders.set(current.id,current);return{...current,idempotentTransition:true,lifecycle:prior}}}throw Object.assign(new Error('Order already exists'),{status:409,code:'ORDER_EXISTS'})}
    let listingBefore=null,listingAfter=null;if(o.listingId){const row=await reserveListingTx(cx,o.listingId);reserved=true;listingBefore={...(row.payload||{}),id:row.id,lotId:row.object_id,sellerId:row.seller_id,status:row.status};listingAfter={...listingBefore,status:'RESERVED'};await enqueueListingStatusTx(cx,listingBefore,listingAfter,{excludeAccountIds:[actor?.id,...watchExcludeAccountIds],eventKey:`order-reserve:${o.id}`})}
    await cx.query('INSERT INTO orders(id,listing_id,object_id,buyer_account_id,seller_id,status,payload,created_at,updated_at) VALUES($1,$2,$3,$4,$5,$6,$7,$8,$8)',[o.id,o.listingId||null,o.lotId,o.buyerClientId,o.sellerId,o.status,o,o.createdAt||now()]);await recordLifecycleTransitionTx(cx,{plan,aggregateId:o.id,sourceKey,metadata:{listingId:o.listingId||null,objectId:o.lotId,buyerAccountId:o.buyerClientId,sellerId:o.sellerId,listingReserved:reserved}});await cx.query('COMMIT');if(reserved)syncListing(o.listingId,'RESERVED');orders.set(o.id,structuredClone(o));return{...structuredClone(o),idempotentTransition:false,lifecycle:plan}
  }catch(e){try{await cx.query('ROLLBACK')}catch{}throw e}finally{cx.release()}
}

export async function transitionOrder(actor,id,next,{lifecycleAuthority=null,lifecycleFacts={},sourceKey=null,patch={}}={}){
  next=String(next||'').toUpperCase();sourceKey=sourceKey==null?null:String(sourceKey);
  if(db.kind!=='POSTGRES'){
    const current=orders.get(id);if(!current||!orderAccess(actor,current))throw Object.assign(new Error('Order not found'),{status:404});const authority=String(lifecycleAuthority||inferLifecycleAuthority({account:actor,resource:current})).toUpperCase(),plan=planLifecycleTransition({domain:'ORDER',from:current.status,to:next,authority,facts:lifecycleFacts}),updated={...current,...patch,status:plan.to};let listingBefore=null,listingAfter=null;if(plan.action==='CANCEL'&&current.listingId){listingBefore=structuredClone(listings.get(current.listingId)||null);await reactivateListingMemory(current.listingId);listingAfter=structuredClone(listings.get(current.listingId)||null)}orders.set(id,updated);await db.putOrder(updated);if(listingBefore&&listingAfter)await emitListingStatusMemory(listingBefore,listingAfter,{excludeAccountIds:[actor?.id]});return{...structuredClone(updated),idempotentTransition:false,lifecycle:plan}
  }
  const cx=await db.pool.connect();let listingReactivated=false;try{
    await cx.query('BEGIN');const row=(await cx.query('SELECT * FROM orders WHERE id=$1 FOR UPDATE',[id])).rows[0];if(!row)throw Object.assign(new Error('Order not found'),{status:404});const current=orderFromRow(row);if(!orderAccess(actor,current)&&!privileged(lifecycleAuthority))throw Object.assign(new Error('Order not found'),{status:404});
    if(sourceKey){const prior=await findLifecycleEventBySourceTx(cx,{domain:'ORDER',aggregateId:id,sourceKey});if(prior){if(prior.to!==next)throw conflict('Lifecycle source key already belongs to a different order transition');await cx.query('COMMIT');orders.set(current.id,current);return{...current,idempotentTransition:true,lifecycle:prior}}}
    const authority=String(lifecycleAuthority||inferLifecycleAuthority({account:actor,resource:current})).toUpperCase(),plan=planLifecycleTransition({domain:'ORDER',from:current.status,to:next,authority,facts:lifecycleFacts}),updated={...current,...patch,status:plan.to};if(plan.action==='CANCEL'&&current.listingId){const li=await reactivateListingTx(cx,current.listingId);listingReactivated=Boolean(li?.status==='RESERVED');if(listingReactivated){const before={...(li.payload||{}),id:li.id,lotId:li.object_id,sellerId:li.seller_id,status:li.status},after={...before,status:'ACTIVE'};await enqueueListingStatusTx(cx,before,after,{excludeAccountIds:[actor?.id],eventKey:`order-reactivate:${id}`})}}await cx.query('UPDATE orders SET status=$2,payload=$3,updated_at=now() WHERE id=$1',[id,plan.to,updated]);await recordLifecycleTransitionTx(cx,{plan,aggregateId:id,sourceKey,metadata:{buyerAccountId:current.buyerClientId,sellerId:current.sellerId,patchKeys:Object.keys(patch||{}),listingReactivated}});await cx.query('COMMIT');if(listingReactivated)syncListing(current.listingId,'ACTIVE');orders.set(id,structuredClone(updated));return{...structuredClone(updated),idempotentTransition:false,lifecycle:plan}
  }catch(e){try{await cx.query('ROLLBACK')}catch{}throw e}finally{cx.release()}
}

export async function captureOrderPaymentAuthority(actor,id,{lifecycleAuthority=null,sourceKey=null,effect}={}){
  if(typeof effect!=='function')throw new TypeError('Order payment effect callback required');sourceKey=sourceKey==null?null:String(sourceKey);
  if(db.kind!=='POSTGRES'){
    const current=orders.get(id);if(!current||(!orderAccess(actor,current)&&!privileged(lifecycleAuthority)))throw Object.assign(new Error('Order not found'),{status:404});if(['PAID','OWNERSHIP_TRANSFERRED'].includes(current.status))return{order:structuredClone(current),idempotentTransition:true,effectResult:null};const authority=String(lifecycleAuthority||inferLifecycleAuthority({account:actor,resource:current})).toUpperCase(),plan=planLifecycleTransition({domain:'ORDER',from:current.status,to:'PAID',action:'CAPTURE_PAYMENT',authority,facts:{PAYMENT_EFFECT_RECORDED:true}}),effectResult=await effect(structuredClone(current));if(effectResult?.paymentEffectRecorded!==true)throw Object.assign(new Error('Payment effect was not recorded'),{status:409,code:'LIFECYCLE_PRECONDITION_FAILED'});const updated={...current,...(effectResult.patch||{}),status:plan.to};orders.set(id,updated);await db.putOrder(updated);return{order:{...structuredClone(updated),lifecycle:plan},idempotentTransition:false,effectResult}
  }
  const cx=await db.pool.connect();try{
    await cx.query('BEGIN');const row=(await cx.query('SELECT * FROM orders WHERE id=$1 FOR UPDATE',[id])).rows[0];if(!row)throw Object.assign(new Error('Order not found'),{status:404});const current=orderFromRow(row);if(!orderAccess(actor,current)&&!privileged(lifecycleAuthority))throw Object.assign(new Error('Order not found'),{status:404});if(['PAID','OWNERSHIP_TRANSFERRED'].includes(current.status)){await cx.query('COMMIT');orders.set(id,current);return{order:{...structuredClone(current),idempotentTransition:true},idempotentTransition:true,effectResult:null}}
    if(sourceKey){const prior=await findLifecycleEventBySourceTx(cx,{domain:'ORDER',aggregateId:id,sourceKey});if(prior){if(prior.to!=='PAID')throw conflict('Lifecycle source key already belongs to a different order transition');await cx.query('COMMIT');orders.set(id,current);return{order:{...structuredClone(current),idempotentTransition:true,lifecycle:prior},idempotentTransition:true,effectResult:null}}}
    const authority=String(lifecycleAuthority||inferLifecycleAuthority({account:actor,resource:current})).toUpperCase(),plan=planLifecycleTransition({domain:'ORDER',from:current.status,to:'PAID',action:'CAPTURE_PAYMENT',authority,facts:{PAYMENT_EFFECT_RECORDED:true}}),effectResult=await effect(structuredClone(current));if(effectResult?.paymentEffectRecorded!==true)throw Object.assign(new Error('Payment effect was not recorded'),{status:409,code:'LIFECYCLE_PRECONDITION_FAILED'});const updated={...current,...(effectResult.patch||{}),status:plan.to};await cx.query('UPDATE orders SET status=$2,payload=$3,updated_at=now() WHERE id=$1',[id,plan.to,updated]);await recordLifecycleTransitionTx(cx,{plan,aggregateId:id,sourceKey,metadata:{buyerAccountId:current.buyerClientId,sellerId:current.sellerId,paymentEffectRecorded:true,patchKeys:Object.keys(effectResult.patch||{})}});await cx.query('COMMIT');orders.set(id,structuredClone(updated));return{order:{...structuredClone(updated),lifecycle:plan},idempotentTransition:false,effectResult}
  }catch(e){try{await cx.query('ROLLBACK')}catch{}throw e}finally{cx.release()}
}

export async function startVerificationAuthority(actor,{id=uid('ver'),caseType=null,subjectType=null,sourceKey=null}={}){
  caseType=caseType|| (actor.accountType==='SELLER'?'KYB':'KYC');subjectType=subjectType||(caseType==='KYB'?'ORGANIZATION':'PERSON');const authority=inferLifecycleAuthority({account:actor}),plan=planLifecycleTransition({domain:'VERIFICATION',from:'NONE',to:'PENDING',action:'START',authority}),ts=now(),v={id,accountId:actor.id,subjectType,caseType,status:plan.to,provider:'NOT_CONFIGURED',providerReference:null,riskLevel:null,decisionReason:'',submittedAt:ts,decidedAt:null,expiresAt:null,createdAt:ts,updatedAt:ts};
  if(db.kind!=='POSTGRES'){await db.putVerification(v);return{verification:v,idempotentTransition:false,lifecycle:plan}}
  const cx=await db.pool.connect();try{await cx.query('BEGIN');await cx.query(`INSERT INTO verification_cases(id,account_id,subject_type,case_type,status,provider,provider_reference,risk_level,decision_reason,submitted_at,decided_at,expires_at,created_at,updated_at) VALUES($1,$2,$3,$4,$5,$6,NULL,NULL,'',$7,NULL,NULL,$7,$7)`,[v.id,v.accountId,v.subjectType,v.caseType,v.status,v.provider,ts]);await recordLifecycleTransitionTx(cx,{plan,aggregateId:v.id,sourceKey,metadata:{accountId:v.accountId,caseType:v.caseType,subjectType:v.subjectType}});await cx.query('COMMIT');return{verification:v,idempotentTransition:false,lifecycle:plan}}catch(e){try{await cx.query('ROLLBACK')}catch{}throw e}finally{cx.release()}
}

export async function transitionVerification(actor,id,next,{lifecycleAuthority=null,lifecycleFacts={},sourceKey=null,patch={}}={}){
  next=String(next||'').toUpperCase();sourceKey=sourceKey==null?null:String(sourceKey);
  if(db.kind!=='POSTGRES'){
    const current=await db.getVerification(id);if(!current)throw Object.assign(new Error('Verification not found'),{status:404});const authority=String(lifecycleAuthority||inferLifecycleAuthority({account:actor,resource:{...current,buyerAccountId:current.accountId}})).toUpperCase();if(!verificationAccess(actor,current)&&!privileged(authority))throw Object.assign(new Error('Verification not found'),{status:404});const plan=planLifecycleTransition({domain:'VERIFICATION',from:current.status,to:next,authority,facts:lifecycleFacts}),updated={...current,...patch,status:plan.to,updatedAt:now()};if(plan.action==='RESUBMIT')updated.decidedAt=null;else if(['VERIFIED','REJECTED'].includes(plan.to)&&!updated.decidedAt)updated.decidedAt=now();await db.putVerification(updated);return{verification:updated,idempotentTransition:false,lifecycle:plan}
  }
  const cx=await db.pool.connect();try{
    await cx.query('BEGIN');const row=(await cx.query('SELECT * FROM verification_cases WHERE id=$1 FOR UPDATE',[id])).rows[0];if(!row)throw Object.assign(new Error('Verification not found'),{status:404});const current=mapVerification(row),authority=String(lifecycleAuthority||inferLifecycleAuthority({account:actor,resource:{...current,buyerAccountId:current.accountId}})).toUpperCase();if(!verificationAccess(actor,current)&&!privileged(authority))throw Object.assign(new Error('Verification not found'),{status:404});
    if(sourceKey){const prior=await findLifecycleEventBySourceTx(cx,{domain:'VERIFICATION',aggregateId:id,sourceKey});if(prior){if(prior.to!==next)throw conflict('Lifecycle source key already belongs to a different verification transition');await cx.query('COMMIT');return{verification:current,idempotentTransition:true,lifecycle:prior}}}
    const plan=planLifecycleTransition({domain:'VERIFICATION',from:current.status,to:next,authority,facts:lifecycleFacts}),updated={...current,...patch,status:plan.to,updatedAt:now()},decidedAt=plan.action==='RESUBMIT'?null:(updated.decidedAt||(['VERIFIED','REJECTED'].includes(plan.to)?now():null));updated.decidedAt=decidedAt;await cx.query(`UPDATE verification_cases SET status=$2,provider=$3,provider_reference=$4,risk_level=$5,decision_reason=$6,submitted_at=$7,decided_at=$8,expires_at=$9,updated_at=now() WHERE id=$1`,[id,plan.to,updated.provider||null,updated.providerReference||null,updated.riskLevel||null,updated.decisionReason||'',updated.submittedAt||null,decidedAt,updated.expiresAt||null]);await recordLifecycleTransitionTx(cx,{plan,aggregateId:id,sourceKey,metadata:{accountId:current.accountId,caseType:current.caseType,provider:updated.provider||null,providerReference:updated.providerReference||null}});await cx.query('COMMIT');return{verification:{...updated,status:plan.to},idempotentTransition:false,lifecycle:plan}
  }catch(e){try{await cx.query('ROLLBACK')}catch{}throw e}finally{cx.release()}
}

export function orderVerificationLifecycleCapabilities(){return{postgresAuthority:db.kind==='POSTGRES',order:{rowLock:true,journal:true,outbox:true,sourceReplay:true,listingReservationAtomic:true,paymentSerialized:true},verification:{rowLock:true,journal:true,outbox:true,sourceReplay:true,reopenable:true}}}
