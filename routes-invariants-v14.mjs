import {PREVIEW,db,send,readBody,requireCsrf,audit,orders,notify} from './runtime-v09.mjs';
import {getDispute,decideDispute,getSettlement,getShipment} from './domain-e2e-v14.mjs';
import {transitionShipment} from './commerce-lifecycle-v16.mjs';
import {postLedgerTransaction,getPayout,listPayouts,transitionPayout} from './finance-v10.mjs';
import {claimProviderEvent,markProviderEventFailed,markProviderEventProcessed} from './provider-events-v15.mjs';
import {inferLifecycleAuthority,planLifecycleTransition} from './lifecycle-authority-v16.mjs';

const SYSTEM={id:'system',roles:['ADMIN'],sellerId:null};
const canOperate=a=>Boolean(a?.roles?.includes('ADMIN')||a?.roles?.includes('TRUST_REVIEWER'));
const providerSecret=()=>process.env.PROVIDER_WEBHOOK_SECRET||'';
async function loadOrder(id){let o=orders.get(id);if(!o&&db.kind==='POSTGRES')o=(await db.pool.query('SELECT payload FROM orders WHERE id=$1',[id])).rows[0]?.payload||null;return o}
async function paidContext(a,d){if(d.orderId){const o=await loadOrder(d.orderId);if(!o)return null;return{amountMinor:Math.round(Number(o.price||0)*100),currency:o.currency||'EUR',provider:o.finance?.provider||'NOT_CONFIGURED',payoutId:o.finance?.payoutId||null,sellerId:o.sellerId||d.sellerId}}if(d.settlementId){const s=await getSettlement(a,d.settlementId)||await getSettlement(SYSTEM,d.settlementId);if(!s)return null;return{amountMinor:Number(s.winningAmountMinor||0),currency:s.currency||'EUR',provider:s.metadata?.provider||'NOT_CONFIGURED',payoutId:s.metadata?.payoutId||null,sellerId:s.sellerId||d.sellerId}}return null}
function refundEntries({sellerId,amountMinor,currency,provider}){return[{ownerType:'SELLER',ownerId:sellerId||'platform-consignment',currency,accountType:'LIABILITY',name:'SELLER_PAYABLE_HOLD',side:'DEBIT',amountMinor},{ownerType:'PAYMENT_PROVIDER',ownerId:provider,currency,accountType:'ASSET',name:'CLEARING',side:'CREDIT',amountMinor}]}
async function reversePayouts(d,ctx){const ids=new Set();if(ctx?.payoutId)ids.add(ctx.payoutId);if(d.orderId)for(const p of await listPayouts({orderId:d.orderId}))ids.add(p.id);for(const id of ids){const p=await getPayout(id);if(p&&['ON_HOLD','READY','FAILED'].includes(p.status))try{await transitionPayout(id,'REVERSED')}catch{}}}
function authorizedProvider(req,res){const secret=providerSecret();if(!secret){send(res,503,{error:'Provider webhook is not configured',code:'PROVIDER_WEBHOOK_NOT_CONFIGURED'});return false}if(String(req.headers['x-provider-secret']||'')!==secret){send(res,401,{error:'Invalid provider webhook secret'});return false}return true}

export async function routeInvariantPublicV14(req,res,url){
 if(url.pathname==='/api/verification/start'&&req.method==='POST'&&!PREVIEW)return send(res,503,{error:'Identity provider adapter is not connected',code:'IDENTITY_PROVIDER_NOT_CONNECTED'});
 if(url.pathname==='/api/providers/identity-events'&&req.method==='POST'){
  if(!authorizedProvider(req,res))return true;const b=await readBody(req),externalEventId=String(b.externalEventId||''),provider=String(b.provider||process.env.KYC_PROVIDER||'NOT_CONFIGURED'),id=String(b.verificationId||b.entityId||'');if(!externalEventId||!id)return send(res,400,{error:'externalEventId and verificationId required'});
  const claim=await claimProviderEvent({providerType:String(b.providerType||'KYC').toUpperCase(),provider,externalEventId,eventType:String(b.eventType||b.status||'EVENT'),entityType:'VERIFICATION',entityId:id,payload:b});if(claim.processed)return send(res,200,{idempotent:true,processed:true});if(!claim.claimed)return send(res,202,{idempotent:true,processed:false,processing:true});
  try{const v=await db.getVerification(id);if(!v)throw Object.assign(new Error('Verification not found'),{status:404});const map={STARTED:'IN_PROGRESS',IN_PROGRESS:'IN_PROGRESS',PENDING:'PENDING',ACTION_REQUIRED:'MORE_INFO_REQUIRED',MORE_INFO_REQUIRED:'MORE_INFO_REQUIRED',VERIFIED:'VERIFIED',REJECTED:'REJECTED',EXPIRED:'EXPIRED'},state=map[String(b.status||b.eventType||'').toUpperCase()];if(!state){await markProviderEventProcessed(provider,externalEventId);return send(res,202,{idempotent:claim.idempotent,recovered:Boolean(claim.recovered),processed:true,application:{applied:false,reason:'EVENT_RECORDED_ONLY'}})}const plan=planLifecycleTransition({domain:'VERIFICATION',from:v.status,to:state,authority:'PROVIDER',facts:{DECISION_SOURCE_VERIFIED:true}});v.status=plan.to;v.provider=provider;v.providerReference=String(b.providerReference||b.externalReference||v.providerReference||'')||null;v.decisionReason=String(b.reason||v.decisionReason||'');v.decidedAt=['VERIFIED','REJECTED'].includes(state)?new Date().toISOString():v.decidedAt;v.updatedAt=new Date().toISOString();await db.putVerification(v);await notify(v.accountId,'VERIFICATION_UPDATED',{verificationId:v.id,status:v.status});await markProviderEventProcessed(provider,externalEventId);return send(res,202,{idempotent:claim.idempotent,recovered:Boolean(claim.recovered),processed:true,application:{applied:true,verification:v,lifecycle:plan}})}catch(e){await markProviderEventFailed(provider,externalEventId,e);throw e}
 }
 if(url.pathname==='/api/providers/payout-events'&&req.method==='POST'){
  if(!authorizedProvider(req,res))return true;const b=await readBody(req),externalEventId=String(b.externalEventId||''),provider=String(b.provider||process.env.PAYMENT_PROVIDER||'NOT_CONFIGURED'),id=String(b.payoutId||b.entityId||''),state=String(b.status||b.eventType||'').toUpperCase();if(!externalEventId||!id)return send(res,400,{error:'externalEventId and payoutId required'});
  const claim=await claimProviderEvent({providerType:'PAYOUT',provider,externalEventId,eventType:state||'EVENT',entityType:'PAYOUT',entityId:id,payload:b});if(claim.processed)return send(res,200,{idempotent:true,processed:true});if(!claim.claimed)return send(res,202,{idempotent:true,processed:false,processing:true});
  try{const p=await getPayout(id);if(!p)throw Object.assign(new Error('Payout not found'),{status:404});if(!['SUBMITTED','PAID','FAILED'].includes(state)){await markProviderEventProcessed(provider,externalEventId);return send(res,202,{idempotent:claim.idempotent,recovered:Boolean(claim.recovered),processed:true,application:{applied:false,reason:'EVENT_RECORDED_ONLY'}})}const next=await transitionPayout(id,state,{provider,providerReference:String(b.providerReference||b.externalReference||'')||null,lifecycleAuthority:'PROVIDER',lifecycleFacts:{PROVIDER_EVENT_OR_PREVIEW:true},sourceKey:externalEventId});const sa=next.sellerId?await db.findAccountBySellerId(next.sellerId):null;if(sa)await notify(sa.id,'PAYOUT_UPDATED',{payoutId:id,status:next.status});await markProviderEventProcessed(provider,externalEventId);return send(res,202,{idempotent:Boolean(claim.idempotent||next.idempotentTransition),recovered:Boolean(claim.recovered),processed:true,application:{applied:true,payout:next}})}catch(e){await markProviderEventFailed(provider,externalEventId,e);throw e}
 }
 return false
}

export async function routeInvariantsV14(req,res,url,ctx){
 if(!ctx)return false;const a=ctx.account;
 const ship=url.pathname.match(/^\/api\/shipments\/([^/]+)\/transition$/);
 if(ship&&req.method==='POST'){
  requireCsrf(req,ctx);const current=await getShipment(a,ship[1]);if(!current)return send(res,404,{error:'Shipment not found'});const body=await readBody(req),next=String(body.status||'').toUpperCase(),providerStates=['IN_TRANSIT','DELIVERED','DELIVERY_FAILED','DAMAGE_REPORTED'];if(!PREVIEW&&providerStates.includes(next))return send(res,403,{error:'Carrier/provider event required for this shipment state',code:'SHIPPING_PROVIDER_EVENT_REQUIRED'});if(!canOperate(a)&&a.sellerId!==current.sellerId)return send(res,403,{error:'Only the seller or operator can advance shipment state',code:'SHIPMENT_AUTHORITY_REQUIRED'});const plan=planLifecycleTransition({domain:'SHIPMENT',from:current.status,to:next,authority:inferLifecycleAuthority({account:a,resource:current}),facts:{PROVIDER_EVENT_OR_PREVIEW:PREVIEW}}),s=await transitionShipment(a,ship[1],plan.to,{...body,lifecycleFacts:{PROVIDER_EVENT_OR_PREVIEW:PREVIEW}});await audit(req,a,plan.auditAction,'SHIPMENT',ship[1],current,{status:s.status},{lifecycle:plan});return send(res,200,{shipment:s})
 }
 const manualPayout=url.pathname.match(/^\/api\/payouts\/([^/]+)\/provider-state$/);if(manualPayout&&req.method==='POST'&&!PREVIEW){requireCsrf(req,ctx);return send(res,503,{error:'Payout state must come from the connected payment provider',code:'PAYOUT_PROVIDER_EVENT_REQUIRED'})}
 const ds=url.pathname.match(/^\/api\/disputes\/([^/]+)\/decision$/);
 if(ds&&req.method==='POST'){
  requireCsrf(req,ctx);if(!canOperate(a))return send(res,403,{error:'Operator permission required',code:'FORBIDDEN'});const before=await getDispute(a,ds[1]);if(!before)return send(res,404,{error:'Dispute not found'});const body=await readBody(req),decision=String(body.status||'').toUpperCase(),isRefund=['FULL_REFUND','PARTIAL_REFUND'].includes(decision),action=decision==='RESOLVED_BUYER'?'RESOLVE_BUYER':decision==='RESOLVED_SELLER'?'RESOLVE_SELLER':decision==='EVIDENCE_REQUIRED'?'REQUEST_EVIDENCE':decision==='UNDER_REVIEW'?(before.status==='EVIDENCE_REQUIRED'?'RESUME_REVIEW':'CONTINUE_REVIEW'):decision==='CLOSED'?'CLOSE':decision;let refund=null;const preflightFacts=isRefund?{REFUND_EFFECT_RECORDED:true}:{},preflight=planLifecycleTransition({domain:'DISPUTE',from:before.status,to:decision,action,authority:'OPERATOR',facts:preflightFacts});if(isRefund){if(!PREVIEW)return send(res,503,{error:'Refund execution requires a connected PSP adapter',code:'PAYMENT_PROVIDER_ACTION_REQUIRED'});const ctxPaid=await paidContext(a,before);if(!ctxPaid?.amountMinor)return send(res,409,{error:'Paid amount is unavailable'});const amount=decision==='FULL_REFUND'?ctxPaid.amountMinor:Math.round(Number(body.refundAmountMinor||0));if(decision==='PARTIAL_REFUND'&&(!amount||amount>=ctxPaid.amountMinor))return send(res,400,{error:'Partial refund must be greater than zero and less than the paid amount'});const provider=ctxPaid.provider==='NOT_CONFIGURED'?'INTERNAL_PREVIEW':ctxPaid.provider,tx=await postLedgerTransaction({transactionType:decision,externalReference:`dispute-${before.id}`,entries:refundEntries({sellerId:ctxPaid.sellerId,amountMinor:amount,currency:ctxPaid.currency,provider}),metadata:{disputeId:before.id,preview:PREVIEW}});refund={ledgerTransactionId:tx.id,amountMinor:amount,currency:ctxPaid.currency};await reversePayouts(before,ctxPaid)}const plan=isRefund?planLifecycleTransition({domain:'DISPUTE',from:before.status,to:decision,action,authority:'OPERATOR',facts:{REFUND_EFFECT_RECORDED:Boolean(refund?.ledgerTransactionId)}}):preflight,d=await decideDispute(a,ds[1],{...body,status:plan.to,refundAmountMinor:refund?.amountMinor||body.refundAmountMinor});await audit(req,a,plan.auditAction,'DISPUTE',d.id,before,{status:d.status,refund},{lifecycle:plan});return send(res,200,{dispute:d,refund})
 }
 return false
}
